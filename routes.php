<?php
/* APDL Routes
 * Set your routing rules here (or don't if you use an add-on routing manager)
 *
 * Pay attention when using overlapping routes, that rules will be searched downwards,
 * so first set the more specific ones, then the less specific ones
 */

//apdl_add_route("example/{strictarg}/[optarg]","module/controller?fixarg=value");
